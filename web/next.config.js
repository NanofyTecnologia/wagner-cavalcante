/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.discordapp.com', 'media.discordapp.net', 'localhost', 'devfy.com.br'],
  },
}

module.exports = nextConfig
